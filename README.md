# Retail-Streaming-Data-Pipeline
An e-commerce checkout service emits order events. The pipeline lands them raw (bronze), cleans and dedupes them (silver), and builds a queryable star schema (gold) that a Spring Boot API exposes to downstream teams.
